import { AfterViewInit, ElementRef, OnDestroy, QueryList, Renderer } from '@angular/core';
import { LinkTo } from './link-to';
import { Router } from './router';
export interface LinkActiveOptions {
    exact: boolean;
}
/**
 * The LinkActive directive toggles classes on elements that contain an active linkTo directive
 *
 * <a linkActive="active" linkTo="/home/page">Home Page</a>
 * <ol>
 *  <li linkActive="active" *ngFor="var link of links">
 *    <a [linkTo]="'/link/' + link.id">{{ link.title }}</a>
 *  </li>
 * </ol>
 */
export declare class LinkActive implements AfterViewInit, OnDestroy {
    links: QueryList<LinkTo>;
    element: ElementRef;
    router$: Router;
    renderer: Renderer;
    activeClass: string;
    activeOptions: LinkActiveOptions;
    private _sub;
    constructor(links: QueryList<LinkTo>, element: ElementRef, router$: Router, renderer: Renderer);
    ngAfterViewInit(): void;
    checkActive(path: any): void;
    ngOnDestroy(): void;
}
